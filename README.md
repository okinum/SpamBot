* commands
1) .Help -> Shows help menu.
2) .Setprefix -> newprefix(string) sets prefix for the selfbot.
3) .Spam -> text(text) times(int) delay(int) {Use '(b)' for a space in text. Use '(rng)' for a random number}.
